[pytest]
testpaths = tests/workflows
asyncio_mode = auto
timeout = 60
markers =
    unit: Unit tests (no external services)
    integration: Integration tests (requires running services)
    smoke: Smoke tests (production health checks)
