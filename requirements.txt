# Core Dependencies
langgraph>=0.0.30
langchain>=0.1.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# YAML Processing
PyYAML>=6.0

# Deployment & Infrastructure
docker>=6.1.0
psutil>=5.9.0
requests>=2.31.0

# Database for Learning System
# SQLite is built into Python, no additional package needed

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0

# Code Quality
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0

# Documentation
mkdocs>=1.5.0
mkdocs-material>=9.0.0

# Optional: For advanced logging
structlog>=23.0.0

# Optional: For monitoring
prometheus-client>=0.17.0

# ─── CI/CD Workflow Test Dependencies (CP-23) ─────────────────────────────────
# HTTP client for workflow tests calling real services
httpx>=0.26.0

# Firebase Admin SDK — for test data cleanup in ci_test_* collections
firebase-admin>=6.4.0

# JWT handling — for generating/validating CI test tokens
PyJWT>=2.8.0

# Test HTML reports — for regression-report.html artifacts
pytest-html>=4.0.0

# JUnit XML parser — for comparing regression results
junitparser>=3.0.0

# Additional pytest features for workflow tests
pytest-timeout>=2.2.0